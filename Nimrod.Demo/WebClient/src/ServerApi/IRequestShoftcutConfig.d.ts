/*
* Class wraps all calls to provider, intercepting success and failure.
* It exposes the IStateReporter interface
*/

declare module Nimrod {
export interface IRequestShortcutConfig extends ng.IRequestShortcutConfig {
}
}